import { Player } from './Player'
import type {
	PlayerInfo,
	PlayerProps,
	PlayerRef,
	Quality,
	SeriesInfo
} from './Player.types'

export { Player }
export type { PlayerProps, Quality, SeriesInfo, PlayerInfo, PlayerRef }
